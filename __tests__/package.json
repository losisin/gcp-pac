{
    "name": "gcp-pac",
    "main": "index.ts",
    "scripts": {
        "test": "pulumi preview --stack dev --policy-pack ../dist/ --policy-pack-config policy.json --suppress-outputs --color always"
    },
    "dependencies": {
        "@pulumi/gcp": "^7.11.2",
        "@pulumi/pulumi": "^3.108.0"
    },
    "devDependencies": {
        "@types/node": "^18.19.21"
    }
}
